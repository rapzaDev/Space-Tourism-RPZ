import { Categories } from '../types';

export function categoriesSamples(): Categories {
    return {
        home: "home",
        destination: "destination",
        crew: "crew",
        technology: "technology"
    };
};